/** Plan con el que arranca todo tenant nuevo durante el trial. */
export const DEFAULT_PLAN_SLUG = 'basico';

/** Base del slug cuando el nombre del negocio no deja ningún caracter usable. */
export const FALLBACK_SLUG_PREFIX = 'negocio';
